<%= title "Todo lists" %>

<% if @todo_lists.size > 0 %>
	<ul class="list todo-lists no-bullet">
	<% @todo_lists.each do |todo_list| %>	
		<%= render partial: 'todo_list_item', locals: {todo_list: todo_list}  %>
	<% end %>
	</ul>
<% else %>
	<%= render partial: 'blank_slate' %>
<% end %>
